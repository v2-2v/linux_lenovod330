# Lenovo IdeaPad D330-10IGM: サスペンド復帰後にDSI内蔵ディスプレイが真っ黒になる問題を徹底的に調べた話

## TL;DR

- Lenovo IdeaPad D330-10IGM（Intel Gemini Lake、内蔵DSIパネル）で、**サスペンド（S3）復帰後に内蔵液晶が真っ黒になる**（バックライトは点くが表示が出ない）不具合を、`i915`ドライバのソースコード解析・カーネル自作パッチの実装と実機投入・Windows用グラフィックスドライバの逆コンパイル・UEFI/BIOSイメージの静的解析まで踏み込んで調べた。
- 調査の結果、GPUドライバ（i915）もUEFIのGOPドライバもWindows用ドライバも、パネル電源制御についてはレジスタレベルまで同一の実装（VBTに書かれたGPIO1本をトグルするだけ）であり、**この経路は本質的に確率的（レースコンディション）にしか成功しない**ことが判明した。一方、電源を完全に切ってから入れ直す本物の電源遷移だけは、OS・ドライバが一切関与しないEC/PMICのハードワイヤードなシーケンスとして実行され、5秒の電源断でも毎回確実に成功する。
- さらにWeb調査で、これがLenovo固有どころかこの1台固有の配線不良ですらなく、**Bay Trail世代から続く、Intelの低消費電力SoC + DSIパネルという組み合わせ全般の構造的な弱点**であり、複数ベンダーの機種で7年以上報告され続けたまま未解決であることも確認した。
- 結論として、**この不具合はカーネルパッチだけで確実には直せない**。実務上のワークアラウンドは「サスペンドを使わず、ディスプレイのオフ/ブランクだけで運用する」こと。これは筆者の思いつきではなく、この機種向けのコミュニティ管理リポジトリでも既に推奨されている対処法である。

長いですが、同じ問題（あるいは似た症状）で困っている人の参考になれば幸いです。

---

## 環境

- 機種: Lenovo IdeaPad D330-10IGM（Windowsタブレット兼用の10.1インチ2-in-1ノート）
- GPU: Intel Gemini Lake, UHD Graphics 600（PCI ID `8086:3185`）
- 内蔵ディスプレイ: DSI-1接続、物理パネルモード `800x1280@60`
- OS: Ubuntu 26.04 LTS
- カーネル: `7.0.0-30-generic`（後述の実験では自前ビルドの `7.0.12-d330custom` も使用）
- デスクトップ: KDE Plasma on Wayland（KWin DRMバックエンド）
- BIOS: 出荷時 `8NCN36WW`（2019年）。調査中に最新版 `8NCN43WW`（2022年）の存在に気づいた。

## 症状

サスペンド（`suspend to RAM`, S3）から復帰すると、

- カーネル・CPU・RAMは正常に復帰する（`PM: suspend exit` まで正常に到達）
- 内蔵LCDだけが復帰しない。バックライトは点灯するが画面には何も表示されない
- DSI-1コネクタは `status: connected`、`enabled: enabled`、`dpms: On` のままで、sysfs上は「正常」にしか見えない

再起動（コールドブート）すれば毎回問題なく表示される。この非対称性が調査の出発点になった。

## 既知の外部情報

同様の症状は、他のD330-10IGM/D330-10IGLユーザーからも複数のディストリビューション（Debian, Fedora, Mint, Arch, ChromeOS Flex）で報告されている（Windowsでは発生しないと報告されている）。この機種専用のコミュニティ管理GitHubリポジトリでは、この問題は現時点で修正不可能とされ、`sleep.target` 等をマスクしてサスペンドを無効化し、代わりにディスプレイのオフ/ブランクを使う回避策が案内されている。またdri-develメーリングリストでも同種の報告（D330-10IGL）があり、Intel側からは正式なi915バグ報告の手順を案内されているのみで、原因分析は提示されていない。

これらの情報から、「Ubuntu固有」「KDE固有」「特定ディストリ固有」の問題ではなく、Gemini Lake + DSIパネル + サスペンド/レジューム、という組み合わせにLinuxカーネル側の何らかの問題がある可能性が高いと判断し、実機での調査に着手した。

---

## 第一段階：i915ドライバのログを詳細に追う

`drm.debug=0x1e`（DRIVER+KMS+PRIME+ATOMIC）を有効にして `rtcwake -m mem -s 20` でS3サスペンド→自動復帰を繰り返し、詳細ログを取得した。

結果、**i915はresume時にDSIパネルの完全な再初期化シーケンス（`intel_dsi_disable()` → `intel_dsi_pre_enable()`）を、一切のエラーログなしに完走している**ことが分かった。

```
[drm:intel_dsi_vbt_exec_sequence [i915]] Starting MIPI sequence 5 - MIPI_SEQ_ASSERT_RESET
[drm:intel_dsi_vbt_exec_sequence [i915]] Starting MIPI sequence 11 - MIPI_SEQ_POWER_OFF
   （サスペンド区間）
[drm:intel_dsi_vbt_exec_sequence [i915]] Starting MIPI sequence 10 - MIPI_SEQ_POWER_ON
[drm:intel_dsi_vbt_exec_sequence [i915]] Starting MIPI sequence 1 - MIPI_SEQ_DEASSERT_RESET
[drm:intel_dsi_vbt_exec_sequence [i915]] Starting MIPI sequence 2 - MIPI_SEQ_INIT_OTP
[drm:intel_dsi_vbt_exec_sequence [i915]] Starting MIPI sequence 3 - MIPI_SEQ_DISPLAY_ON
[drm:intel_dsi_vbt_exec_sequence [i915]] Starting MIPI sequence 6 - MIPI_SEQ_BACKLIGHT_ON
```

`grep -iE 'error|timeout|fail'` してもDSI関連のエラーは一件もヒットしない。DSI PHY/データレーンのリンク確立（`GLK_PHY_STATUS_PORT_READY`等のタイムアウトチェック）もログ上は全て成功している。EDIDが空なのは正常起動時も同じ仕様（DSIパネルはDDC/EDIDを持たない）で、これはバグの直接証拠にはならない。

つまり、**「ドライバの視点では成功しているのに、物理的にはパネルが反応しない」**という厄介な性質のバグであることが早々に判明した。

ACPIピン（GPIOコントローラ `INT3453`、DSDT解析）を辿り、パネル電源シーケンスで操作されているGPIOがPMIC/I2C経由ではなくSoC内蔵の純粋なGPIOであることも確認し、PMIC resumeレースという仮説も棄却した。

## 第二段階：カーネルを自前ビルドして遅延を操作する実験

D330実機と同一バージョンのUbuntuカーネルソースを取得し、`intel_dsi_vbt.c` に実験用の `module_param` を4つ追加した独自パッチを作成。パネル電源シーケンス（`MIPI_SEQ_ASSERT_RESET`/`DEASSERT_RESET`/`POWER_ON`/`POWER_OFF`）の各ステップ直後に、実行時に値を変更できる `msleep()` を挿入し、カーネル・モジュールを再ビルドして実機に投入した。

さらに、**サスペンドを一切経由しない `kscreen-doctor --dpms off/on` だけで、S3復帰時と全く同じ症状（バックライトは点くが真っ黒）が再現する**ことを発見した。これにより、以降の実験は「S3サスペンド＋十数秒待ち」ではなく「DPMSオフ→オン、数秒」で行えるようになり、検証サイクルが劇的に高速化した。同時に、真の原因は「サスペンド固有の何か」ではなく、**i915の`intel_dsi_disable()`→`intel_dsi_pre_enable()`というDSI無効化→再有効化サイクルそのもの**に内在することが分かった。

このカスタムパラメータを使い、電源OFF直後の待機時間（`power_off_settle`）と電源ON後の待機時間（`power_on_settle`）を体系的に振ってみたところ：

- 遅延なし（VBT標準値のまま）：ほぼ0%
- 350〜2000ms：体感5〜7割で成功（ただし同一条件でも成功/失敗がばらつく）
- **60秒・120秒まで延ばしても改善なし（0/3で失敗）**

「単純な待機不足ではなく、確率的なハードウェアレベルのマージン不足（レース状態）である」という結論に至った。

## 第三段階：Windowsは何が違うのか——ドライバの逆コンパイル

「Windowsでは発生しない」という報告を受け、Lenovo公式サイトから配布されているこの機種向けのIntelグラフィックスドライバ（`igdkmd64.sys`）を、実機にWindowsをインストールせずに静的解析した（Inno Setupインストーラーを展開し、Ghidraで逆コンパイル）。

パネル電源制御に関わりそうな関数（`CSLBASE_SetPanelPower`、`MIPISINK_GetPanelPowerSeqTimingDelay`等）をデコンパイルした結果：

- VBT（ファームウェア埋め込みの表示初期化データ）のうち、Linux側の構造体定義に含まれていない未使用フィールドが2つ存在することを発見したが、実機のVBTデータを直接ダンプして確認したところ**その値はゼロ**であり、本件の原因ではなかった。
- `CSLBASE_SetPanelPower` は単なる薄いディスパッチャで、実処理は仮想関数経由で別オブジェクトに委譲されており、Control Flow Guard（CFG）による間接呼び出しの難読化のため、実際の実行部隊（VBT MIPIシーケンスの解釈・GPIO操作）にはたどり着けなかった。

さらに、GPIOコントローラ用のIntel Serial-IOドライバ（`iaLPSS2_GPIO2.sys`）も同様に取得・解析したが、Windowsの汎用GPIOクラス拡張（GpioClx）のコールバック実装は単純なレジスタread-modify-writeのみで、**待機・ポーリング・検証ロジックは一切なく、そもそも「待つ」ためのカーネルAPIすらインポートされていなかった。**

この時点では「Windowsは何か賢いことをしているのでは」という仮説は否定的な材料ばかりで、はっきりした答えは出なかった。

## 第四段階：決定的な勘違いに気づく——「コールドブートは成功する」の意味

ここまでの調査はすべて「コールドブートは常に成功する」ことを、i915のDSI初期化コード自体は正しく動くという根拠にしてきた。しかし、これが誤解だった。

`drm.debug=0x1e` を**GRUBの起動オプションとして最初から**（sysfs経由ではなく）有効にし、ログイン前の起動シーケンス自体を確認したところ：

```
[drm:simpledrm_device_create...] display mode=...
[drm] fb0: simpledrmdrmfb frame buffer device
   （i915ロード後）
[drm:intel_fbdev_driver_fbdev_probe [i915]] re-using BIOS fb
[drm:intel_crtc_state_dump [i915]] [CRTC:77:pipe A] enable: yes [fastset]
```

**「Starting MIPI sequence」のログが一件も出現しない。** `fastset` は、i915が「ハードウェア状態が既に望ましい状態と一致している」と判断し、フルモードセット（＝DSI再初期化シーケンス）を丸ごとスキップする仕組みである。

つまり、コールドブート時にパネルを実際に点灯させているのは**i915ではなくUEFI/BIOSのGOP（Graphics Output Protocol）ドライバ**であり、i915は単にBIOSが用意したフレームバッファをそのまま引き継いでいるだけだった。DPMSオフ→オンやサスペンド復帰の時だけ「fastset requirement not met, forcing full modeset」となり、初めてi915自身のDSI初期化コードが実行される。

**「i915のDSI再初期化ロジックが実際に成功した確認済みの実例」は、この調査を通じて一度も存在しなかった可能性が高い。**

## 第五段階：UEFI GOPドライバを直接解析する

「コールドブート時に本当に動いている実装」であるUEFI GOPドライバの実体を確認するため、Lenovo公式サイトから配布されているBIOSアップデートパッケージ（Insyde H2O形式）をダウンロードし、オープンソースのUEFIツール（UEFITool）でファームウェアイメージを展開した。

この中に `IntelGopDriver`（UEFI DXEドライバ、89KB）を発見し、Ghidraで全関数をデコンパイルした。VBT MIPIシーケンスのバイトコードインタプリタ本体を特定でき、その構造（opcode番号：1=SEND_PACKET, 2=DELAY, 3=GPIO, 4=I2C, 5=SPI, 6=PMIC）はLinuxの `intel_dsi_vbt_exec()` と完全に一致していた。

決定的だったのは、GPIO設定関数の実体：

```c
undefined8 FUN_00013174(ushort gpio_index, short value)
{
    ...
    // コミュニティベースアドレス + パッドオフセットからMMIOアドレスを計算
    volatile uint32_t *reg = ...;
    if (value == 1) {
        *reg = *reg | 1;          // ON: ビットセット
    } else if (value == 0) {
        *reg = *reg & 0xfffffffe; // OFF: ビットクリア
    }
    return 0;
}
```

**単純な32bitレジスタのread-modify-writeのみ。読み戻し確認・ポーリング・追加の暗黙的待機は一切ない。** つまり「常に成功する参照実装」であるはずのGOPドライバも、GPIO操作についてはLinuxのi915と機能的に完全に同一だった。副産物として、パネルVBTを含む4種類のBIOS埋め込みVBTを比較し、実機に搭載されているパネルの型番（"KD101N67HD"系）を初めて特定できた。また、BIOSアップデート前後でVBTデータは1バイトも変わっていないことも確認した（アップデートで直る可能性は否定された）。

## 第六段階：時間を操作する実験と、決定的な反証

「GPIO一本による電源OFFでは、DDIC（パネル駆動チップ）の本当のコア電源が落ちていないのではないか」という仮説（＝副次的な電源レールだけを切っており、コア電源は別経路の常時通電レギュレータから供給され続けている）を検証するため、カスタムカーネルの `power_off_settle` を極端に延長する実験を行った。

| 電源OFF時間（GPIO単体トグル） | 結果 |
|---|---|
| 60秒 ×2 | 失敗 |
| 120秒 | 失敗 |

改善の兆候が全くなかった。ここまでは「GPIO index 3は本当のコア電源を制御していない」という仮説を支持する結果に見えた。

しかし、これとは別に、**完全に `poweroff` した状態から電源ボタンを"単押し"すると出る、バッテリー充電状態の簡易ロゴ画面**（OSを起動せず、UEFI/EC層の最小限の処理だけで表示される機能）について調べたところ、興味深い事実が判明した。**電源を切ってからわずか5秒後に単押ししても、このロゴ表示は毎回確実に成功する。**

GPIOトグルのみで120秒（2分）待っても失敗するのに対し、本当の電源断からはわずか5秒で成功する——この非対称性から、**「電源OFF時間の長さ」自体は本質的な変数ではない**ことが分かった。

## 第七段階：パネルメーカーを特定し、決定的な補強証拠を見つける

BIOSイメージから抽出した4種類のVBT（`KD101N67HD`／`KD101N56FHD`／`TV101WUMFHD`／`TV101WXMHD`）について、それぞれのメーカー・型番をさらに調べた。

- **`TV101WUM*`系** → **BOE**（京東方科技集団）製と判明。10.1インチ・MIPI-DSI接続で、実効表示エリア（135.36mm×216.576mm）は実機の報告値ともほぼ一致する。Linuxカーネルには既にこのパネルファミリー向けの汎用DRMパネルドライバ（`panel-boe-tv101wum-nl6.c`）が存在し、ARM系SoC搭載のChromebook等で広く使われている。
- **`KD101N*`系** → **Kingdisplay** 製である可能性が高い。dri-develメーリングリストのパッチ履歴に、"KD"を冠したKingdisplay製パネル（`kd110n11-51ie`、`kd101ne3-40ti`等）が同系統のドライバ・専用ドライバとしてLinuxにマージされている実績があった。

ここで、このARM版汎用ドライバの電源投入シーケンス（`boe_panel_prepare()`）を確認したところ、**それまでの仮説を強力に裏付ける決定的な事実**が見つかった。このドライバは、パネル電源投入に**4系統の独立したレギュレータ**を要求している：

```c
struct regulator *pp1800;  // 1.8V デジタル電源
struct regulator *avee;    // 負極性アナログ電源（LCD駆動電圧、負側）
struct regulator *avdd;    // 正極性アナログ電源（LCD駆動電圧、正側）
struct regulator *pp3300;  // 3.3V（一部モデルのみ）
```

投入順序も厳密に規定されている：

```c
regulator_enable(boe->pp1800);
usleep_range(3000, 5000);
regulator_enable(boe->avdd);
regulator_enable(boe->avee);
usleep_range(10000, 11000);
// ここで初めてreset/enable GPIOをトグルする
```

**つまりこの同系統パネルの正規の電源投入手順は、単純な「1本のGPIOで電源をON/OFF」ではなく、1.8Vデジタル電源とAVDD/AVEEという2系統のアナログ駆動電圧を、個別に・順序立てて投入する必要がある。**

一方、GLK/D330側のVBT（`mipi_pps_data`）は、`panel_on_delay`／`bl_enable_delay`／`bl_disable_delay`／`panel_off_delay`／`panel_power_cycle_delay`という、**単一の電源系統しか想定していないモデル**である。GeminiLakeのVBTが「GPIO index 3 = POWER」として一括制御しているのは、実際にはこの複数レールのうちの一部（おそらく1.8Vデジタル系統だけ）に過ぎず、**AVDD/AVEEに相当するアナログ駆動電圧は、VBT/GPIOの管理下にない別経路（SoC/PMIC側の固定電源、あるいはEC/PMICが電源投入時にのみハードウェアシーケンスするレール）から供給され続けている**のではないか、という仮説が、パネルファミリー自体の公開リファレンス実装という、実機データに依存しない独立した証拠から補強された形になる。

## 第八段階：GPIO index 5（pin 70）の実測とパッチ検証、そして前提の訂正

方針を「検証と修正」に絞り、直前の仮説（GPIO index 5＝pin 70がAVDD/AVEE相当の未制御レールではないか）を、実機での直接観測とカーネルパッチの両方で検証した。

`libgpiod`で実機のGPIOを直接読み取ったところ、**pin 70は誰にも要求されていない`input`状態のまま、常に`active`（高）であり続けていた**。`kscreen-doctor --dpms off/on`の全期間（POWER用GPIOが確実にOFF→ONしたことをログで確認済み）にわたって100ms間隔でポーリングしても、一度も値が変化しなかった。これは「常時通電の未制御レールが存在する」という仮説を実測で裏付ける結果に見えた。

そこで、VBTの`MIPI_SEQ_POWER_OFF`/`POWER_ON`の前後でGPIO index 5をPOWER（index 3）と連動させて開閉する実験パッチを実装し、3回検証した。**3回とも失敗**し、明確な改善は確認できなかった。ただしこの時点では、既にあらゆる既存手法の成功率が原因不明のまま大きく低下していた（後述）ため、「仮説自体が誤り」なのか「たまたまその時期だけ成功率が落ちていたのか」は判別できなかった。

その後、「UEFIは複数GPIOを使って正しく起動しているのでは」という疑問がふと浮かび、再検証したところ、**重大な論理的矛盾**に気づいた。GOPのVBTシーケンス実行部は、Linuxの`intel_dsi_vbt_exec()`と opcode番号まで完全に同一のロジックで、同一のVBTバイトコード（36/43WWで1バイトも差異なし）を解釈している。一方、本調査で取得した全ての詳細ログを通じて、GPIO index 1・2・5を操作する命令は一度も観測されていない。**つまりVBTバイトコード自体に、そもそもこれらのGPIOを操作する命令が含まれていない**——GOPも全く同じデータを解釈している以上、GOPもこれらのGPIOを操作していないはずである。「GOPは複数GPIOを正しく操作しているが、i915はそれを怠っている」という前提は誤りだった。

これにより、pin 70が常時ONである理由は、**VBT/GOP/i915いずれのソフトウェアの制御範囲外——SoCのピンマルチプレクサのハードウェアデフォルト状態、あるいはVBTより前に動作するファームウェアのシリコン初期化コードにある**、という、より悲観的な評価に修正した（この直前のパッチが3連敗したこととも整合する）。

この訂正と並行して、候補DDIC「Himax HX8394」について、公式データシート（HX8394-F、2014年12月版）の実物をWeb調査で発見した。**5.12.2節「外部電源供給モード」に、まさに求めていた電源投入・切断シーケンス図が明記されていた**：

```
【電源投入順序】
VDD1 → (>1ms) → VSP → (>1ms) → VSN
RESX（リセット解除） → (>10us) → SLPOUT → (>120ms) → DISPON

【電源切断順序】
SLPIN → (≥2フレーム待機) → VSP/VSN → Hi-Z or GND
```

`VSP`/`VSN`（正負のアナログ駆動電圧）は、前段で確認したARM版汎用パネルドライバの`avdd`/`avee`に相当し、**それぞれの投入には最低1msの間隔が公式に規定されている**——単純な「1本のGPIOで一括ON/OFF」というモデルでは、この規定順序を原理的に満たせない。ただし、このIC自体は外部からVSP/VSNを供給する「外部電源モード」と、チップ内蔵チャージポンプが自動生成する「内部生成モード」を設定ピンで切り替えられ、**D330の実装がどちらのモードなのかは汎用データシートだけでは特定できない**（実機のFPCコネクタのピン数・配線を見る必要がある）。内部生成モードであれば「外部の未制御レール」という概念自体が成立せず、EC/PMIC内部の完全に不可視な経路という、より説明の難しい仮説に説明を求める必要が出てくる。

## 第九段階：DDIC自己診断コマンドの実験——読み取り自体がタイムアウトし、判定不能

配線の話は一旦脇に置き、バックライトのみになる証拠を標準プロトコルのレベルから直接取れないか、と考え直した。HX8394データシートにある標準MIPI DCSコマンド`Get_power_mode`（RDDPM）・`Get_diagnostic_result`（RDDSDR）を使い、DDICチップ自身に自己診断結果を問い合わせるパッチを実装・実機投入した。

3回検証したが、**3回とも黒画面失敗、かつ3回ともDSIホストコントローラ自体が`Timeout waiting for read data`を報告し、実際のDDIC応答ではなくタイムアウト時のゼロ埋めデータしか得られなかった**。成功例と失敗例を比較するという本来の目的は果たせず、「MIPI DSIの読み取り（逆方向通信）経路自体が、このプラットフォーム/ドライバの組み合わせでは機能していない可能性がある」という、パネルの成否とは独立した制約が疑われる結果になった。

## 第十段階：`fastset`を強制無効化する決定的実験——コールドブートでもi915自身のDSIコードは不安定だった

第四段階の発見（「i915自身のDSI再初期化コードが実際に成功した確認済みの実例は、本調査を通じて一度も存在しない」）を直接検証するため、`intel_crtc_check_fastset()`に強制フラグ`d330_force_full_modeset`を追加し、GRUBコマンドラインから有効化した。これにより、通常はBIOSのフレームバッファをそのまま引き継いでスキップされる、**i915自身のDSI無効化→有効化フルシーケンスを、コールドブート中に初めて実行させる**ことができた。

1回目の試行：**GOPと全く同じ「真に電源断からの初回実行」という条件であるにもかかわらず、本調査を通じて何百回も見てきたあの黒画面症状がそのまま再現した。**

ただしここで一つ気になったのは、「そもそもGOP自身も初期化をスキップしているのではないか（i915のfastsetと同種の仕組みがGOP内部にもあるのでは）」という点だった。GOPの呼び出し元を3階層遡ってGhidraで追跡したところ、GOP自身にも「要フル初期化」判定は存在するが、それは**DXEフェーズでGOPが新規ロードされるたびにゼロクリアされる、GOPプロセス内部だけの状態**であり、i915のfastsetのような「ファームウェアの別フェーズが既にやってくれたか」というOS/ファームウェア境界をまたぐ判断ではないことが分かった。つまり通常の1回のコールドブートでは、GOPは毎回律儀にVBTシーケンス本体を実行しており、「GOPは実は初期化をスキップしている」という説は支持されなかった。

そこで`d330_force_full_modeset=1`のテストを`grub-reboot`で計6回繰り返したところ、**6回中1回（約17%）しか成功しなかった**。GOPが本調査を通じて数十〜100回近いコールドブートで事実上100%の成功率を維持してきたことと明確に乖離しており、**「GOPとi915は同じVBTデータ・同じopcode解釈ロジック・同じ実行条件（初回実行）で動いているのに、成功率がまるで違う」という、単なる「G3を経由するかどうか」だけでは説明できない事実**が統計的に裏付けられた（副産物として、成功した1回でもDCS読み取りは同様にタイムアウトしており、前段の読み取り不良はパネルの成否とは無関係なプラットフォーム側の制約だと確定した）。

## 第十一段階：ログ解析からDSI PLLロック所要時間という新しい相関変数を発見（ただしサンプル僅少）

10章で得た6回分の完全なdmesgを比較したところ、**成功例と失敗例はログの行レベルでは全く区別できなかった**（エラー・警告も皆無）。しかし、`MIPI_SEQ_POWER_OFF`開始から`DSI PLL locked`までの実測時間（ハードウェアのロック検知ビットをポーリングして得られる、`msleep`ではない実測値）を計測すると：

| # | 結果 | POWER_OFF→PLL locked |
|---|---|---|
| 1〜3, 5, 6 | 失敗 | 396〜613 ms |
| 4 | **成功** | **261.5 ms（6回中最短）** |

**唯一の成功例が、最も短いPLLロック時間だった。** PLLロックの実測時間が、その瞬間の電源レール・基準クロックの安定度など電気的コンディションを反映する代理指標であり、「ロックが速い＝電気的に良好→後続のDDIC初期化も成功しやすい」という仮説を示唆する結果である。これは、これまでの「固定時間、盲目的に長く待つ」という修正の方向性（第二・第六・第八段階）とは異なる、「観測してリトライする」という新しい修正方針の着想につながった。ただしn=6・成功1例のみという極めて小さいサンプルであることには注意が必要である。

## 第十二段階：決定的な発見——i915はGOPと全く同じハードウェア準備完了チェックを持つが、結果を握りつぶしていた（しかし黒画面の直接原因ではなかった）

GOPが呼んでいる、ポーリング＋タイムアウトの構造を持つ関数をさらにデコンパイルしたところ、20msの予算でMMIOレジスタの特定ビットをポーリングし、タイムアウトすると明示的なエラーコードを返して呼び出し元に伝播させ、クリーンな後片付けに入っていることが分かった。このレジスタ・ビットをi915のソースと突き合わせたところ、`BXT_MIPI_PORT_CTRL`のbit17`AFE_LATCHOUT`（D-PHYアナログフロントエンドのレディネスビット）と完全一致した。

i915側の対応コード（GLK/D330が実際に使う`glk_dsi_device_ready()`）を確認すると、**GOPと全く同じ4つのハードウェアレディネスチェック**（PHYステータス・ULPS非アクティブ・データレーンStop状態・AFE_LATCHOUT）を持っていたが、**タイムアウトしても`drm_err()`でログを出すだけで、関数は正常終了し、呼び出し元には一切失敗が伝わらない**ことが判明した。DSI PLLロック確認（`bxt_dsi_pll_enable()`）にも全く同じパターンがあった。これは、本調査を通じて繰り返し確認してきた「i915は失敗時も一切エラーを出さず、常に成功したかのように処理を完了する」現象の、直接的かつ具体的な原因に見えた。

しかし、これが実際に観測された黒画面失敗の**直接原因かどうか**を確認するため、実機に保持されていた過去30ブート分（本調査のほぼ全ての実機試行を含む）のログを、これらのチェックが出すはずの`drm_err()`文字列（`PHY is not ON`等5種類）で全数検索した。**結果：全30ブート・0件。これらのエラーログは一度も発火していなかった。**

つまり、握りつぶし自体はコード品質上の実在する問題だが、**過去の全ての失敗試行で、D-PHY/PLLのレディネスチェック自体は常にタイムアウト予算内で正常に完了していた**ことになる。これにより、実際の失敗点は、これより後段——VBT MIPIシーケンスが送信する、DDIC向けのDCSコマンド列（`MIPI_SEQ_INIT_OTP`・`MIPI_SEQ_DISPLAY_ON`）の実行過程——に絞り込まれた。この領域はGOP・i915のどちらにも一切のハードウェア確認（読み戻し・ACK確認）が存在せず、かつ前段で確認した通りDCS読み取り自体がプラットフォーム側の制約で機能しない。**つまり現状、ソフトウェア側から見える範囲では、この最終段階の失敗を検知する手段が何一つ存在しない**、というのが本調査全体を通じての実質的な結論になった。

## 第十三段階：GOP的リトライ機構の実装・実機投入

GOPのようなリトライ機構を自分でも実装してみることにし、前段で発見した握りつぶし問題を実際に修正するパッチを書いた。`bxt_dsi_pll_enable()`・`glk_dsi_device_ready()`の戻り値を`void`から`bool`に変更して実際の判定結果を呼び出し元に伝播させ、失敗時は明確なログを出した上でクリーンな後片付け（reset assert→power off→PLL disable）を行い、短い待機後にリトライ（最大3回、`module_param`で調整可能）。成功時は追加ログなしで従来通り処理を継続する設計にした。

実機投入して4回試行：1回目は成功（リトライ発火せず）、2回目は失敗——**しかし前段の予測通り、この失敗もパッチの検知範囲（PLL/D-PHY）の外側で起きており、リトライは発火しなかった**。3・4回目は1敗1勝で、通算2勝2敗（成功率50%）。第十段階までの17%よりは高いが、n=4では有意な改善とは言い切れない。

## 第十四段階：PLLロック時間の閾値ベースリトライを追加——しかし初回データが第十一段階の仮説と矛盾

第十一段階の相関を踏まえ、`d330_retry_pll_threshold_ms`（デフォルト320ms）を追加し、ブリングアップ所要時間がこれを超えたら同じリトライ経路に合流させるパッチを実装した。あわせて計測区間を「PLL enable開始〜device ready完了」という、1回の試行内で完結するより狭い区間に変更し、閾値の有効/無効にかかわらず毎回の所要時間を無条件でログ出力するようにした。

実機投入後の初回試行：**所要時間わずか56ms**（第十一段階の最速成功例261.5msよりさらに速く、320msの閾値には遠く及ばない）にもかかわらず、**実際に画面を確認すると「バックライトのみ、真っ黒」**——失敗だった。これは「ブリングアップが速ければ成功する」という第十一段階由来の仮説に対する直接的な反証であり、n=1ではあるが、その相関が測定区間の違いによるものか、あるいは統計的なノイズだった可能性を強めた。

## 第十五段階：DPMS高速試行サイクルの確立と、「DPMSは浅いリセット・リブートは深いリセット」という新知見

再起動（約45秒）を待たずにサンプル数を増やすため、`kscreen-doctor --dpms off/on`をSSH経由でKDE Waylandセッションに対してリモート実行する手順を確立し、これが実際に同じコードパス（`intel_dsi_pre_enable()`）を数秒で再実行させられることを確認した。

成功直後に同じ起動セッション内でDPMSサイクルを4回連続実行したところ、**4回連続で全く同じパターン（毎回ほぼ同一の所要時間・失敗）を再現し、一度も自己回復しなかった**——これは、再起動ベースの試行で見られた試行ごとのばらつき（17%・50%）とは明確に異なる挙動だった。この直後にリブートし直すと即座に成功に転じたことも確認した。ここから、**「DPMSレベルの電源オフ/オンは"浅い"リセットであり、DDIC内部の異常状態からは抜け出せない。真の意味でのコールドスタートに相当する電源断（リブート、あるいは第六段階で確認した実際のEC経由の電源断）でなければリセットされない」**という新しい仮説が浮上した。これは、リトライ機構（第十三・十四段階）自体の後片付け処理がGPIOレベルの浅いサイクルに過ぎないため、原理的に黒画面から回復できない可能性を示唆する、重要な——そして悲観的な——傍証でもある。

並行して、以前示していた「成功期間はログの行数密度が高い（218.6行/秒 vs 25〜62行/秒）」という比較についても再検討した。しかし「ログの量が変わるならそれ自体がソフトウェアからの検知になるのでは」と考え直して調べ直したところ、**この比較には交絡があった**——「成功期間」に使ったのがリブート・ログイン直後で、パネルの状態とは無関係なデスクトップ起動時特有の活動（ウィジェット読み込み等）が大量発生する窓であり、「失敗期間」はその活動が既に収まった後の窓だった。定常状態のベースラインはほぼ無音（21秒で1行程度）であることを確認し、同一条件での追試を試みたが、**追試そのものの中で再起動が失敗してしまい**、同一条件のサンプルを揃えられないまま、この日はここで区切りをつけた。**「ログ行数が間接的な検知シグナルになりうるか」は、明確な結論の出ないまま未決着に終わっている。** 翌日、頭を冷やしてから仮説そのものを客観的に洗い直すところから調査を再開した。

## 第十六段階：仮説を客観的に洗い直す——PSR/PSR2とSLPIN待機不足を、確度高く除外する

それまでの流れを見直し、まだ潰していなかった仮説を優先順位順に検証し直すことにした。

**PSR/PSR2仮説** — Windowsのグラフィックスドライバ側で"T3 Optimization"という省電力機構が意図的に無効化されていたことを思い出し（第三段階参照）、PSR (Panel Self Refresh) がGeminiLake+DSIの構成に関与している可能性を確認した。

実機のdebugfsを覗くと：

```
$ cat /sys/module/i915/parameters/enable_psr
-1
$ ls /sys/kernel/debug/dri/0000:00:02.0/DSI-1/
edid_override  force  i915_lpsp_capability  output_bpc  vrr_range
$ ls /sys/kernel/debug/dri/0000:00:02.0/DP-1/
... i915_psr_status  i915_psr_sink_status ...
```

**DSI-1コネクタにはPSR関連のdebugfsエントリが一切存在しない**（DP-1側にのみ存在する）。カーネルソースでも、

```
$ grep -n "connector_type != DRM_MODE_CONNECTOR_eDP" display/intel_psr.c
3411:  if (conn->connector_type != DRM_MODE_CONNECTOR_eDP)
```

と、`intel_psr.c`はeDP/DisplayPortコネクタのみを対象にしていることが確認できた。GeminiLake+DSIの構成では、PSR機構はそもそもコード上コネクタに配線されていない。実機再起動を伴う検証をするまでもなく、この経路は除外できる。

**SLPIN後の待機不足仮説** — HX8394データシートの電源切断シーケンス（`SLPIN → (≥2フレーム待機) → VSP/VSN off`）を踏まえ、`MIPI_SEQ_DISPLAY_OFF`完了直後に追加の待機を挿入する実験パラメータを実装したが、1回の試行だけでは判断がつかなかったため、VBTのMIPIシーケンスブロック自体をバイナリレベルで正しく解析するスクリプトを新しく書き、実機のVBT実データを直接デコードしてみた。

```
--- sequence 4 (DISPLAY_OFF) size=29 ---
  SEND_PKT data=000502002800   (Set Display Off, 0x28)
  DELAY 200000 us
  SEND_PKT data=000502001000   (Sleep In / SLPIN, 0x10)
  DELAY 100000 us
  => total explicit DELAY: 300.0 ms

--- sequence 11 (POWER_OFF) size=12 ---
  DELAY 100000 us
  GPIO index=3 value=144  (POWER off)
  => total explicit DELAY: 100.0 ms
```

VBTのバイトコード自体が、SLPIN送信後にすでに100ms（データシート要件「≥2フレーム、約33ms」の3倍）の待機を規定しており、`POWER_OFF`前にもさらに100ms待機している。これは第二・第六段階で確認した「待機時間をいくら延ばしても意味がない」というパターンと完全に整合する結果であり、この仮説も実データによって反証できた。

## 第十七段階：`glk_cold_boot`という分岐を発見する——しかし「レース条件」の域を出なかった

`intel_dsi_pre_enable()`の実装を読み直していたところ、Gemini Lake専用の重要な分岐に気づいた。`glk_dsi_enable_io()`が返す真偽値`glk_cold_boot`によって、DSIプロトコル設定一式を書き込む`intel_dsi_prepare()`を、ハードウェアレディネスチェック`intel_dsi_device_ready()`の**前**に呼ぶか**後**に呼ぶかが切り替わっていたのだ。

```c
glk_cold_boot = false;
if (display->platform.geminilake) {
    glk_cold_boot = glk_dsi_enable_io(encoder);
    /* Prepare port in cold boot(s3/s4) scenario */
    if (glk_cold_boot)
        intel_dsi_prepare(encoder, pipe_config);
}
if (!intel_dsi_device_ready(encoder))
    ok = false;
/* Prepare port in normal boot scenario */
if (display->platform.geminilake && !glk_cold_boot)
    intel_dsi_prepare(encoder, pipe_config);
```

コメントに明示的に「cold boot(**s3/s4**) scenario」と書かれている。原作者がまさにサスペンド/レジューム相当のケースを想定して用意した分岐だった。

`glk_cold_boot`の判定材料になるレジスタ値と分岐結果をログするパッチを実機投入し、同一ブート内でコールドブート試行とDPMS試行を比較したところ：

```
attempt 1: MIPI_DEVICE_READY before enable_io=0x00000000, glk_cold_boot=0  ← コールドブート、成功
attempt 1: MIPI_DEVICE_READY before enable_io=0x00000000, glk_cold_boot=1  ← DPMS off/on、失敗
```

**判定材料のレジスタ値は両方とも`0x00000000`で完全に同一なのに、`glk_cold_boot`の結果は0と1に分かれた。** これは真にハードウェアの冷/温状態を検知しているのではなく、`glk_dsi_enable_io()`内部の「IO電源投入→Pwr ACK待ち」の間で決まる、タイミング依存のレース条件だった可能性が高い。

これを固定できれば直せるのでは、と考え、`glk_cold_boot`の値を強制的に「成功時と同じ0」に固定するパッチを実装・検証したが、4回中1回しか成功せず、これまでのベースライン（約17%）と有意な差は出なかった。**構造的には実在する興味深い分岐だが、これ単体が黒画面バグの根本原因ではない、という結論に落ち着いた。**

## 第十八段階：Modern Standby (s2idle) を試す——回避できない上に、全く別の失敗経路が見つかる

Windowsが使っているModern Standbyという省電力方式なら違うかもしれない、と考え、Linuxでのそれに相当する`s2idle`を試した。

まずカーネルソースを確認すると、i915自体は`s2idle`と従来のACPI S3（`deep`）を区別するコードを持っている。しかし実際にDSIパネルの無効化・再有効化を引き起こしているのはDRM汎用ヘルパーの`drm_atomic_helper_suspend()`で、これは`s2idle`/`deep`の区別を一切見ずに、アクティブな全CRTC/エンコーダを無条件に無効化する。復帰側でも、

```c
/* Force recalculation even if we restore current state. */
crtc_state->mode_changed = true;
```

と、毎回無条件でフルモードセットを強制する。つまりi915の設計上、**どちらのスリープモードを選んでも、DSIパネルの無効化→再有効化サイクル自体は回避できない**ことがソースレベルで確認できた。

実機でも`s2idle`に切り替えて検証したところ、案の定バックライトのみで真っ黒——ただし、これまで仕込んできた計装ログ（`Starting MIPI sequence`や独自の計装ログ）が**一件も出力されなかった**。これは「ログが出なかっただけ」ではなく、**`intel_dsi_pre_enable()`自体が一度も呼ばれなかった**ことを意味する。`i915_display_info`を見ると、ソフトウェア上は`enable=yes, active=yes`と「正常に復帰した」ことになっているにもかかわらず、実際の画面は真っ黒——これまで十数段階かけて追いかけてきたGPIOシーケンスの経路を一度も通らずに、**全く別の、これまで捕捉できていなかった経路で**同じ症状に到達することが新たに分かった。この経路については、本調査では深追いできていない。

## 第十九段階：Web調査で判明した、最も重い事実——これはLenovo固有でも、この1台固有でもなかった

ここまでの調査は、暗黙のうちに「D330というこの機種固有の設計不良ではないか」という前提で進めてきた。しかし改めてWeb調査を行ったところ、その前提自体が崩れた。

まず、Lenovo以外の全く別ブランドの機種でも、同一構成（Gemini Lake + MIPI DSI内蔵タッチパネル）で類似の症状が報告されていた。廉価版2-in-1タブレット「EVOO EV-T2in1-101-2」（Lenovo製ではない）に関するGitHub Issueには、「Lenovo D330も同一の10.1インチ1280x800パネル（AUO製の別パネル型番）で同様の不具合が報告されている」という記述まであった。しかも、このIssueには**「`drm.debug=0x1e`を有効にすると画面が正常になる」**という報告があり、デバッグログ出力がタイミングを変えることで症状が変わる——これはまさにレースコンディション仮説を裏付ける状況証拠だった。

さらに世代を遡ると、Gemini Lakeより前のBay Trail世代でも、freedesktop.orgのバグトラッカーに同種の報告が複数見つかった。

| バグ番号 | 対象 |
|---|---|
| `101205` | [i915][BYT] Blank screen on DSI panel |
| `96923` | Zen10タブレット（無名ブランド） |
| `93799` | Teclast X80hタブレット |
| `82880` | Dell Venue 8 Pro |
| `89704` | WinBook TW700タブレット |

**Bay Trail→Cherry Trail→Gemini Lakeという複数世代、Lenovo・EVOO・Teclast・Dell・WinBookという複数ベンダーにまたがって、「Intel低消費電力SoC + DSIパネル + i915ドライバ」という組み合わせで、同種の黒画面バグが繰り返し報告され続けていた。**

Gemini Lake DSI専用のバグ`109267`（2019年、D330系列のパネルについて）のステータスは**「RESOLVED MOVED」**——修正されたのではなく、単なる別トラッカーへの事務的な移管を意味する。「このバグを"GLK DSIの一般的な問題"として一括りに扱うのを避けるため、あえてクローズする」という趣旨の記述もあり、Intelが繰り返しの報告を把握しつつも、公式に「これはi915の既知バグでXXが原因」と認めた形跡は見当たらなかった。個別報告のたびに、技術的な原因分析なしの「正式な手順で再提出してください」という定型対応で処理され続けているのが実態のようだった。なお、Gemini Lakeの別のサスペンド関連バグ（DMCファームウェアの再プログラム漏れ）は実際に修正された実績があり、Intelに直す能力・意思がないわけではなさそうだった。7年以上未解決なのは、「直す気がない」からというより、「誰も決定的な根本原因を提示できていない」ことが大きいのだろう。

## 第二十段階：実基板の回路図を入手し、EC型番とパネル電源経路を直接確認する

この記事を公開した後も調査を続けていたところ、この機種の実装ベースとなっている基板メーカーが作成した社内向けの回路図一式を、修理系のコミュニティ経由で参照する機会に恵まれた。詳しい入手経路はここでは伏せるが、実機を分解しなくても部品番号・信号名・レジスタレベルの配線を直接確認できる、これまでで最も確度の高い一次資料である。この基板の開発コードネームは**「T6066」**であることも、この資料から分かった（Lenovo側の型番D330-10IGM/D330-10IGLとは別に、基板設計側で使われている社内呼称）。

回路図から、EC本体が**ITE製「IT8987」**（128pinのLPC接続チップ）であることを型番レベルで確認できた。これまでACPI解析で間接的にしか触れられていなかったECの実体が、ようやく特定できたことになる。

## 第二十一段階：パネル電源経路の回路図レベルでの裏付け——新しい反証ではなく、既存仮説の補強だった

回路図でもっとも重要だったのは、DSIパネルの電源・バックライト回路そのものが載っていたページである。確認できたのは：

- **パネル本体の電源レールは、単純なロードスイッチ1個で、SoC側のGPIO1本によってON/OFFされているだけだった。** ECが管理するシート（電源シーケンス表）ではなく、SoC GPIOのシートを参照しており、これはACPI解析・VBT GPIOトグルという既存の結論と、回路図レベルで完全に一致した。
- **ECが管理する電源シーケンス表（`G3→S5→S4/S3→S0`の順にチップセット電源を投入していく、まさに以前の段階で仮説として提示していたものそのもの）には、パネル電源に関する信号が一件も含まれていなかった。** つまり回路図の上でも、「ECが確実に管理する電源シーケンス」と「SoC GPIO1本に依存する不安定なパネル電源」は、設計上明確に別系統として分離されていることが確認できた。

これは新しい反証ではなく、**推測に頼っていた仮説を、メーカー一次資料で裏付けた**という位置づけになる。

さらに、バックライトの有効化回路の近くに、ECに由来すると思われる信号線が1本見つかった。正確な接続先までは特定できていないが、もしバックライトの点灯経路にECが（パネル本体の電源とは独立に）関与しているのだとしたら、**「バックライトは点灯するが画面には何も表示されない」という、この記事全体を貫く症状と構造的に整合する**——バックライト系統とパネル映像系統が回路的に分離しており、後者だけが確実に失敗している、という説明になる。

## 第二十二段階：正常系（コールドブート）の初期化フローを、全証拠を統合して再構成する

ここまでのソフトウェア解析（VBTバイトコードのデコード、GOP/i915ソースコードの突き合わせ）と、今回入手した回路図を統合し、電源投入（G3）からバックライト点灯までの全フローを時系列で整理し直した。

```
[G3] 完全電源断
  │ EC(IT8987)がハードワイヤードでチップセット電源を投入
  │ （DSIパネル固有のレールはこの管理下に含まれない）
  ▼
UEFI起動、DXEフェーズでGOP(グラフィックスドライバ)がロード
  ▼
GOPがVBTのMIPIシーケンス（バイトコード）を実行
  ├─ パネル電源ON（SoC GPIO1本のトグル）
  ├─ リセット解除
  ├─ DSI物理層(D-PHY)・PLLのブリングアップ
  ├─ DDIC初期化コマンド送信
  ├─ 表示開始コマンド送信
  └─ バックライトON（SoC GPIO、場合によってはECの信号とも合流）
  ▼
[画面表示成功] 以降i915はこの状態をそのまま引き継ぐだけで、
  同じ初期化コードを実際には実行しない
```

このフロー整理を通じて改めて浮き彫りになったのは、**「パネルを実際に点灯させる操作」自体は、コールドブートでも、失敗するS3/DPMS復帰でも、一言一句同じ**という事実である。違うのは、その操作が実行される瞬間の周辺の電気的状況だけ、という結論を、より具体的な信号名・部品名のレベルで裏付ける形になった。

## 第二十三段階：「ならばOS側から直接GPIOを操作すれば直せるのでは」を検証し、手詰まりを確認する

回路図が手に入ったことで、「まだ見つかっていない、隠れた制御ピンがあるのでは」という可能性を初めて実地に検証できるようになった。回路図全ページのテキストを、パネル駆動チップの複数電源系統（正負のアナログ駆動電圧等）に関するキーワードで全文検索したが、**パネル周辺には一件もヒットしなかった。** これは、この基板には、複数の電源レールを個別に順序立てて投入するための、追加のGPIOやレギュレータが物理的に存在しないことを意味する。

つまり、ソフトウェア（カーネルドライバ）が制御できる電源ピンは、これまで散々実験してきた3本（リセット・電源・バックライト）で全てであり、回路図によって「これ以上の隠れた制御点はない」ことまで確認できてしまった。**OS側からのGPIO操作というアプローチは、これで実質的に手詰まりであることが、状況証拠ではなく回路図によって確定した**、というのが今回の調査で得られた、地味だが重要な結論である。

## 第二十四段階：EC単体のファームウェアも入手できたが、改造は筋が悪いという結論に至る

回路図と同じ経路で、EC単体の生ファームウェア（`ITE EC-V14.6`という版数の8051系マイコンコード、128KB）も参照する機会があった。これを解析・改造してパネルの電源シーケンスを直接制御できないか、という発想は自然に浮かぶが、**前段で確認した通り、ECの既存シーケンスはそもそもパネル電源に一切触れていない。** つまり改造で作るとすれば、既存の良いコードを復活させるのではなく、ゼロから新規に書く必要があり、そのコードが最終的に叩けるのは、結局これまでソフトウェアが叩いてきたのと全く同じ、たった1本のGPIOに過ぎない。しかも、これまでのカーネルモジュールの実験と違い、**EC書き込みの失敗には安全網がなく、最悪の場合は起動不能（文鎮化）に直結する。** 見込みの薄いリターンと、質的に重いリスクを天秤にかけ、この方向には進まないことにした。

## 第二十五段階：「なぜECが直接パネルを初期化しているように見えていたのか」の整理、そして"サスペンドを直す"ことの原理的な限界

調査を通じて、「本物の電源断→起動（経路②）だけが確実に成功する」という表現が、「ECがパネルを起動している」という誤解を招きやすいことに気づいた。正確には、**ECはパネルに一切関与せず、チップセット全体をゼロから起こすだけである。その"ゼロから起こす"という電気的な文脈の中で、パネル用の単純なGPIOトグルがたまたま高確率で成功する**、という間接的な関係にすぎない。

この理解に立つと、「OSのサスペンド（S3）から復帰する際に、カーネルが工夫してこの"良い電気的文脈"を再現できないか」という発想も、実は原理的に不可能だと分かる。**S3は、RAMの内容を保持するために一部の電源レールを"あえて生かしたまま"にするからこそ高速復帰を実現している省電力モードであり、定義上、真の電源断（G3）には決してなり得ない。** 「S3並みの高速復帰」と「G3並みの電気的品質」は、両立しない要求なのである。

一方、**ハイバネート（S4）は、作業内容をディスクに退避してから本当に電源を落とす仕組み**であり、復帰時は文字通り経路②（真の電源断→起動）を通る。もしこれが正しく動けば、「サスペンドに近い使い勝手＋確実な画面復帰」が両立できる可能性がある。当初の調査で試みたハイバネート検証は、本件とは別のOS起動基盤の問題（`resume=`パラメータが認識されない）でブロックされてしまい、棚上げにしていた。これを別途掘り下げる価値がありそうだ。

## 第二十六段階：次にやりたいこと——オシロスコープによる実電圧波形の測定

ここまでの調査は、ソフトウェアのログと、今回入手した回路図という「静的な設計情報」の突き合わせが中心だった。**唯一まだ手を付けていない、そして最も決定的な証拠になり得るのが、パネルへの実際の電圧波形をオシロスコープで直接観測することである。** 次にやりたいこととして、測定計画をここに書き残しておく。

**最優先の測定対象は、パネルに実際に届く電源レール（回路図上でロードスイッチの出力側にあたる、パネルコネクタ直前のポイント）である。** ここで見たいのは主に2点：

1. ディスプレイをオフ（DPMS off）にした状態や、サスペンド中に、この電圧が**本当に0Vまで落ちきっているか**。もし落ちきっていない、あるいは非常にゆっくりとしか下がらないなら、「GPIO1本のトグルだけでは、パネル駆動チップを完全にリセットするための電位まで下げきれていない」という仮説を、初めて定量的に裏付けられる
2. 電源を再投入したときの**立ち上がり波形そのもの**（スルーレート、途中での乱れの有無）。これを、画面が実際に映った試行と映らなかった試行とで比較し、波形に違いがあるかを見る

次点で見たいのは、リセット信号（パネルコネクタのリセットピン）の実波形と、前回の調査で見つかった「バックライトの点灯経路に絡んでいそうな、EC由来と思われる信号線」のタイミングである。後者は、「バックライトはDSIの初期化成否と無関係に、ECが独自のタイミングで点けているのではないか」という仮説を直接検証できる。

なお、DSIの信号線自体（クロック・データレーン）の波形取得は、数GHz帯域の差動プローブと専用のプロトコルデコード機能が必要になり、一般に入手可能な機材の範囲を超えるため、今回は対象外とする。

測定にあたっては、ACアダプタ経由でのグラウンドループを避けるためバッテリー駆動のみで行う必要があること、測定点がいずれも極小サイズの部品の近くにあるため、あらかじめ細いリード線をハンダ付けしてから筐体を閉じる必要があることも、実施前に留意しておきたい。

**この測定で「電圧が0Vになりきっていない」ことが確認できた場合**、さらにその原因（電源スイッチIC自体のリーク電流なのか、あるいはパネルコネクタの別のピン——常時通電されたままのIDピン等——からの逆流なのか）を切り分ける必要が出てくる。後者だった場合、対策には基板側への追加の配線改造が必要になり、ソフトウェアだけでは完結しない話になる可能性が高い。

---

## 最終的な結論

ここまでの調査で確度高く言えることを整理する。

**GeminiLake（あるいはBay Trail世代以降のIntel低消費電力SoC）搭載機のDSI内蔵パネルには、OS/GPUドライバから制御できる電源投入手段が、事実上「VBTに書かれたGPIO1本のトグル」しか用意されていない。** これはWindows用グラフィックスドライバ・UEFI GOPドライバ・Linux i915ドライバのすべてで、レジスタ操作レベルまで見ても機能的に同一の実装だった。パネル駆動チップ（DDIC）本来の電源投入シーケンス——本機と同系統のパネルファミリー向けにLinuxに既に存在する汎用ドライバを見る限り、1.8Vデジタル電源とプラス/マイナス2系統のアナログ駆動電圧を、個別に・順序立てて投入する必要がある——を、この1本のGPIOトグルだけで代替できているとは考えにくい。

一方で、**電源を完全に落としてから起動しなおす「本物のG3→S0電源遷移」だけは、EC（組み込みコントローラ）/PMICがOS・ドライバの一切関与しないハードワイヤードな手順として実行しており、わずか5秒の電源断でも毎回確実に成功する。**

```
経路①：GPIO1本のトグルだけで再投入する（i915・UEFI GOP、両方とも共通のアルゴリズム）
        → DPMSオフ/オン、サスペンド復帰（s2idle/S3を問わず）は、すべてこの経路
        → 確率的に失敗する（電気的マージン不足によるレースコンディション）

経路②：完全な電源断(G3)→起動(S0)。EC/PMICのハードワイヤードなシーケンス
        → OS・ドライバは一切関与しない
        → 5秒の電源断でも毎回確実に成功する
```

つまりこのバグは、「GeminiLakeやi915に画面初期化能力が欠けている」という単純な話ではなく、**「専用の電源シーケンサIC（パネルPMIC）が搭載されていない廉価な設計において、EC/PMICが電源投入時にだけ肩代わりしている複雑な仕事を、サスペンド復帰時に代替する手段がソフトウェア側に一切用意されていない」**という、より構造的な設計限界だと理解するのが最も事実に整合する。

そしてこれはLenovo D330というこの1機種、この1台の配線ミスの話ではない。Bay Trail世代から数えて3世代・複数ベンダーにまたがって同じ症状が報告され続けており、2019年に一度Intelの正式なバグトラッカーに上がった際も、根本原因の特定にも修正にも至らず「RESOLVED MOVED」という事実上の店ざらしで終わっている。7年以上、この種の格安DSIタブレット/2-in-1機種を作り続けてきたベンダー側にもIntel側にも、この問題を本気で潰しにいくだけの商業的な動機が働かなかったのだろう。

**結論として、この不具合はカーネルパッチや設定変更だけで確実に解決できる類のものではない。** 実際にi915のソースコードを読み込み、独自のリトライ機構や複数のタイミング調整パッチを実装して実機に投入してみたが、いずれも決定的な改善には至らなかった。本気で直すなら、EC/PMICのファームウェア側から、サスペンド復帰時にも本物の電源投入シーケンスをもう一度発火させる——つまりOSからはほぼ制御できない領域に踏み込む必要があり、量産から何年も経った1機種のためにそこまでのリソースが投じられる見込みは薄い。

## 実務上のワークアラウンド

サスペンドを使うのをやめるのが、現実的かつ唯一確実な対処法である。D330専用のコミュニティ管理GitHubリポジトリでも同じ結論に達しており、実際に推奨されている。

```bash
sudo systemctl mask sleep.target suspend.target hibernate.target hybrid-sleep.target
```

でサスペンド系のtargetをマスクし、**サスペンドは使わず、アイドル時はディスプレイのオフ/ブランクのみで運用する。** ディスプレイオフは経路①(GPIOトグル)を経由しないため、この問題自体が発生しない。

`kscreen-doctor`のDPMSオフ/オンでも同じ症状が起きることは確認済みなので、「画面がしばらくすると自動で消える」設定にしている場合、消灯後の自動復帰でも同じ黒画面に遭遇しうる。その場合は自動消灯そのものをオフにし、手動でロック画面から復帰する（＝実質、経路①を踏ませない）運用にするのが安全である。

なお、本当にどうしても画面が戻らなくなった場合も、**再起動（経路②）は毎回確実に画面を復帰させる**ので、パニックにならず`sudo reboot`すればよい。DPMSレベルの黒画面は同一ブート内では自己回復しないが、リブートすれば必ず直ることは実験的に確認済みである。

## 残された謎

すべてが解明できたわけではない。気になる人のために、未解決のまま残っている点を挙げておく。

- **s2idleで見つかった、全く別の失敗経路**（第十八段階）。GPIOシーケンス自体が一度も実行されないまま黒画面に到達する経路があり、原因は未特定のまま。
- **Windowsで「蓋を閉じて開ける」と復帰する理由。** ①蓋を閉じた直後はまだパネル電源断（経路①）に至っておらず、単なるバックライト消灯の段階ですぐ開けている可能性、②Windowsのランタイム電源管理がLinuxよりきめ細かく、経路①の実行自体を回避している可能性、の2つを疑っているが、いずれも実証はできていない。
- **パネルコネクタの実電圧波形は、結局一度も実測していない。** 回路図の入手により、どのテストポイントを・どういう手順で測るべきかまでは具体的に計画できた（電源スイッチの出力側で、DPMS off/S3中に本当に0Vまで落ちているか、落ちきらないなら他の常時ONの信号線からの逆流ではないか、という仮説まで立てられている）が、実測そのものはまだ行えていない。複数ベンダー・複数世代で同じ症状が出ている以上、実証できても原因はこの1台の配線ではなく上流の設計限界にある可能性が高く、修正につながる見込みは薄いと判断してはいるが、upstreamへの報告の説得力を上げる材料としては着手する価値が残っている。
- **「なぜたまに成功するのか」の具体的なメカニズムは、依然として未解決。** 確率的な電気的マージン不足というカテゴリまでは特定できたが、個々の成否を予測する変数（DSI PLLロック時間等）はいずれも検証の結果、決め手にならなかった。同一条件下でも成功率自体がセッションによって大きく変動する現象の原因も特定できていない。
- **ハイバネート（S4）を正しく動かせないか。** ハイバネートは実際に電源を落として起動し直す仕組みなので、理屈の上では経路②（確実に成功する側）を毎回通るはずである。今回の調査で試みたハイバネート検証は、本件とは別のOS起動基盤側の問題（`resume=`パラメータ関連）でブロックされてしまい、深掘りできていない。もし直せれば、サスペンドに近い使い勝手と、確実な画面復帰を両立できる可能性がある。
- **LinuxのVBTパーサー（`struct mipi_pps_data`）の構造体定義の不整合。** Windows側が読んでいる7つのタイミング値のうち末尾2つがLinux側では定義すらされていないことを確認した。本件D330では値がゼロだったため直接の原因ではなかったが、他機種のVBTでは非ゼロ値が入っている可能性もあり、独立した話としてカーネルコミュニティに指摘する価値はあるかもしれない。

## おわりに

最終的にたどり着いたのは、「直せるバグを見つけて直す」という当初のゴールではなく、「これは直せない種類のバグだと、根拠を持って言い切れるようになった」という結論だった。i915のソースコードを読み、Windowsのグラフィックスドライバを逆コンパイルし、UEFIのBIOSイメージまで解析し、自作のカーネルパッチを何本も実機に投入した末に得たのは、「専用のパネル電源ICを積んでいない廉価機種では、サスペンド復帰時にDSIパネルを確実に再起動する手段が、そもそもソフトウェア側に存在しない」という、身も蓋もない事実だった。

同じ機種、あるいは同じ症状（Gemini Lake以前のIntel低消費電力SoC + DSI内蔵パネルでのサスペンド復帰失敗）の環境を使っている方は、無理にサスペンドを直そうとせず、本記事のワークアラウンドで運用することをおすすめする。もし電圧実測やEC解析まで踏み込んでくれる方がいれば、ぜひ結果を教えてほしい。何か進展があれば、`qiita-d330@yoimiya.net` まで連絡をもらえると嬉しい。

## 参考

- 本機種専用のコミュニティ管理GitHubリポジトリ（サスペンド無効化のワークアラウンドを推奨）
- dri-devel メーリングリストのD330-10IGL関連スレッド
- LinuxQuestions / Arch Linux Forum の関連報告
- `sebanc/brunch` リポジトリのIssue（EVOO EV-T2in1-101-2での類似報告）
- freedesktop.org Bugzilla: `109267`（GLK DSI, D330系）、`101205`（BYT）、`96923`（Zen10）、`93799`（Teclast X80h）、`82880`（Dell Venue 8 Pro）、`89704`（WinBook TW700）

（本記事は個人の調査記録であり、Intel/Lenovoの公式見解ではありません。誤りや追加の知見があればぜひ教えてください。）
